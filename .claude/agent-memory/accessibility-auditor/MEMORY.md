# Accessibility Auditor Memory — NeonBinder

- [Contrast reference for the neon palette](contrast-reference.md) — measured ratios for every neon token and slate gray against the app's real dark backgrounds; stop re-deriving these by hand. Now also covers amber/gray-200/600/700 OKLCH conversions, the "no dark: split at all" bi-theme bug, and per-theme translucent-badge contrast (NEO-102)
- [NeonButton contrast defect (pre-existing, site-wide)](neonbutton-contrast-defect.md) — `secondary` and `cancel` variants fail 1.4.3; affects every page that uses them, not just the one being audited
- [Focus-park pattern already established in this codebase](focus-park-pattern.md) — where it's used correctly, and the gaps found in review-grid.tsx (NEO-152), a second busy-gated button in MissingTeamFixer, and an auto-triggered dialog with no antecedent click (both NEO-102)
- [role/aria-live consistency for always-mounted live regions](live-region-role-pattern.md) — the one correct way and the one bug found doing it wrong
- [Nested opacity contrast math + aria-pressed-pair→radiogroup pattern](nested-opacity-contrast-and-radiogroup.md) — how to compute contrast through chained `bg-*/NN` layers, and the mutually-exclusive-toggle fix from CardPairingModal (NEO-189)
- [Nested-dialog focus trap + restore](nested-dialog-focus-trap.md) — a confirm dialog inside another dialog's ref leaks Tab focus and drops focus on close unless it traps/restores itself; plus an unfixed cross-modal `document.activeElement` staleness case (NEO-203)
- [rAF-based focus-park effects: guard on activeElement===body, and the test-timing trap](raf-focus-park-race.md) — an unconditional "no fixer this render" park effect over-fires; how to test it without a stale mount-effect rAF winning a false race (NEO-102)
- [Shared component on two surfaces: unconditionally-dark dialog vs bi-themed panel](shared-component-two-surface-contrast.md) — one fixed hex set can't serve both; `surface` prop pattern, duplicated-JSX gotcha, Enter-bound-to-input focus strand, list-none in Safari, locked-test-pinned aria-label (NEO-101)
- [Label-in-name substitution vs insertion + async-swap focus loss + forced-aria-label info-drop](label-in-name-async-swap-pattern.md) — three related NEO-212 findings: word substitution fails 2.5.3 even when the codebase's usual word-insertion pattern doesn't; a near-match query resolving can swap a primary button's DOM shape and strand focus; an unconditional aria-label silently hides a sibling confidence badge from AT users
- [WCAG 2.5.8 Target Size — recurring under-24px pills, icon buttons and underline links](target-size-2.5.8.md) — concrete padding fixes and how to compute the effective hit-area height (NEO-211)
- [Shared live region + shared refocus target needs cross-disabled triggers](shared-row-actions-mutual-exclusion.md) — two per-row async actions feeding one role=status region must mutually disable, or a race drops an announcement (4.1.3); plus the `p-2 -m-2` target-size convention and the pre-existing non-unique-recipient-name limitation (NEO-121)
- [Neon palette contrast ratios](neon-palette-contrast.md) — measured 4.5:1/3:1 pass-fail for every neon-on-dark pair; reuse instead of recomputing.
- [Recurring WCAG findings in admin master-detail screens](wcag-recurring-findings.md) — busy-state aria-label mismatches, over-announcing counters, focus lost on in-panel remounts, color-only toggle states. Check these first on Players/Teams/Leagues-shaped screens.
- [Contrast patterns](contrast-patterns.md) — gray-500 on dark bg fails (~3:1–3.7:1); gray-400 or gray-600/dark:gray-400 fixes it; Tailwind dark: is media-query-driven, not tied to Radix's forced dark appearance
- [NeonButton `cancel` variant contrast](neonbutton-cancel-contrast.md) — white-on-#FF2E9A ≈3.44:1, fails AA; design-system issue, don't patch per-call-site
- [Target size & aria-disabled patterns](target-size-and-focus-patterns.md) — px-1-only small buttons miss 24x24; codebase's own aria-disabled-over-native-disabled convention (NeonButton supports both) and where BaseSetPicker.tsx doesn't follow it
- [Autosave-per-field drawer patterns](autosave-per-field-drawer-patterns.md) — Escape bypassing blur silently drops an autosave-on-blur edit (keyboard-only); native `disabled` on a shared-busy-flag button group, 4th confirmed occurrence; an `<option>`'s visible text is its only reliable accessible name; a component with errors wired but zero success feedback becomes a real 4.1.3 defect once blank becomes a meaningful outcome (NEO-216/217)
- [Admin master-row aria-label pattern](patterns_admin_master_row_aria_label.md) — Maestro forces `aria-label` = exact resource-id string; fix hidden state/glyph info via `aria-describedby`, never touch the label
- [Measured contrast ratios for this project](reference_contrast_ratios_measured.md) — real bg is `#0a0a0a`, tailwind.config.js hex values differ slightly from CLAUDE.md prose; slate-400/neon-orange pass comfortably, neon-pink passes AA not AAA
- [Status-message / live-region pattern](patterns_status_message_live_regions.md) — role="alert" refusal + aria-describedby preview (no aria-live) is this codebase's correct, established pattern; verify clear-before-retry for repeat-identical-error re-announcement

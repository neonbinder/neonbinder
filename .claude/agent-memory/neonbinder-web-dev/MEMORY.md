# NeonBinder Web Dev — Agent Memory Index

- [Disambiguate duplicate aria labels by rewording](disambiguate-duplicate-aria-labels-by-rewording.md) — When two instances of the same picker/component can be on screen at once, give the second instance labels that share NO substring with the first — never a suffixed var…
- [E2e viewport is the ux constraint](e2e-viewport-is-the-ux-constraint.md) — Admin screens are judged at the CI E2E viewport (1024x629); put status/feedback next to the control that produced it, because role="status" hides sighted-user gaps
- [External ids must be verified live](external-ids-must-be-verified-live.md) — Wikidata/ESPN QIDs and other external identifiers shipped as constants must be checked against the live source before merge — four sportConfig hallOfFameQid values wer…
- [Linking uis are drag and drop](feedback-linking-uis-are-drag-and-drop.md) — Any "these two lists describe the same things" pairing/linking UI must be drag-and-drop, and must say so on screen — Jason's operator feedback on NEO-189 / PR #205
- [No inline sub object forms](feedback-no-inline-sub-object-forms.md) — Owner rejects inline fields that create a DIFFERENT object inside a form; use a modal that completes the sub-object first, then selects it
- [Feedback no self opening modals](feedback-no-self-opening-modals.md) — Never let a modal open itself off a background/reactive event — advertise the work in a live region and let the operator press a button
- [Feedback notice ctas need a structural marker](feedback-notice-ctas-need-a-structural-marker.md) — Extra affordances on a shared status banner must key off an explicit marker on the notice state, never the tone bucket or the message text
- [Feedback team location is any place including a school](feedback-team-location-is-any-place-including-a-school.md) — Team `location` is wherever the team is from — city, state, region OR school; never write that colleges/national sides "have no location
- [Midbuild security conditions](feedback_midbuild_security_conditions.md) — On this project a security-auditor pass lands mid-build as numbered conditions that OVERRIDE the launching prompt; fold them in immediately and report per-condition wi…
- [Pendingteamnames invisible until sync](pendingteamnames-invisible-until-sync.md) — A quick-added card's typed team used to land in pendingTeamNames, which no checklist UI rendered and NEO-102 also un-badged — fixed and merged in NEO-208 (PR #215); on…
- [Bsc attach only functions on insert rows](project_bsc_attach_only_functions_on_insert_rows.md) — Known gap found in NEO-196 — a row's attached BSC ids only affect card fetching on `insert` rows; on variantType and parallel rows they are attribution-only. SportLots…
- [Eslint flat config skips ts](project_eslint_flat_config_skips_tsx.md) — apps/web ESLint lints .tsx (react-hooks + the NEO-44 raw-input rule) but still NOT plain .ts — a .ts file reports "File ignored because no matching configuration was s…
- [Selector deadline scope](project_selector-deadline-scope.md) — NEO-198 deliberately did NOT bound the credential/re-auth path behind the selector-option adapters; do not re-propose that surgery.
- [Adapter mocked fixtures hide gate divergence](reference_adapter_mocked_fixtures_hide_gate_divergence.md) — Checklist tests that vi.mock the whole BSC/SL adapter let a seeded chain model a shape the real adapter refuses — so tightening a chain-level gate surfaces failures in…
- [Apps web root tsc is red at baseline](reference_apps_web_root_tsc_is_red_at_baseline.md) — root tsc is red at baseline AND no gate typechecks components/app/src — grep the root run for your own .tsx files; an empty grep is the gate
- [Clerk ui strings live in clerk ui package](reference_clerk_ui_strings_live_in_clerk_ui_package.md) — Clerk's prebuilt-component text is NOT in node_modules — clerk-react loads clerk-js by MAJOR from Clerk's CDN and the UI (plus every en-US string) ships in a separate…
- [Convex call spread hides bad args](reference_convex_call_spread_hides_bad_args.md) — Spreading conditional keys into a useMutation/useAction call bypasses TS excess-property checking, so a Convex arg the validator will reject at runtime compiles clean
- [Convex codegen only blocks types](reference_convex_codegen_only_blocks_types.md) — Missing Convex codegen blocks ONLY tsc, never vitest/convex-test — api.js is anyApi/componentsGeneric proxies, so new functions and components resolve at runtime witho…
- [Convex components unregistered in convex test](reference_convex_components_unregistered_in_convex_test.md) — No test in apps/web calls t.registerComponent, so draining any schedule that reaches a component (wikidataPool, workpool) swaps EnvironmentTeardownError for "Component…
- [convex-test transactionLimits pin the read set](reference_convex_test_transaction_limits_pin_the_read_set.md) — `documentsRead` caps turn a read RANGE into an assertion; the only way to test an OCC narrowing whose answer is unchanged
- [Convex one paginate per function](reference_convex_one_paginate_per_function.md) — Convex + convex-test allow ONE .paginate() per execution; a multi-node chunked walker pages with .take() + a _creationTime cursor instead
- [Convex test creationtime fake timer clamp](reference_convex_test_creationtime_fake_timer_clamp.md) — convex-test stamps _creationTime from Date.now() but clamps it strictly-increasing, so testing a time-based sweep with fake timers needs a base AFTER real wall-clock n…
- [Reference field marker class is two classes and mangles camelcase](reference_field_marker_class_is_two_classes_and_mangles_camelcase.md) — An apps/web Input carries TWO mb-field-* classes (the primitive's own plus the caller's fieldClass key), and useFieldTestClass mangles a camelCase key — "cardNumber" b…
- [Fresh worktree missing env local](reference_fresh_worktree_missing_env_local.md) — A fresh apps/web worktree has no .env.local, so every route renders a blank black page and the only clue is a ClerkProviderBase warning — not a code bug
- [Load only network guard trip is a delayed tail](reference_load_only_network_guard_trip_is_a_delayed_tail.md) — A NEO-188 guard trip that fails only in the full parallel suite and passes in isolation is usually a self-rescheduling DELAYED scheduled tail that drainScheduled canno…
- [Maestro multi select unreachable](reference_maestro_multi_select_unreachable.md) — Maestro's web driver can only tap options in the FIRST native <select> on a page — never build a Maestro-covered view with more than one <select>; use button lists
- [Maestro web frame stall kills scrolling](reference_maestro_web_frame_stall_kills_scrolling.md) — maestro-web's only scroll primitive is a frame-driven smooth scroll, so when headless Chrome's compositor stalls on the drilled set-builder page every scroll silently…
- [Playermanagement deeplink test flakes under load](reference_playermanagement_deeplink_test_flakes_under_load.md) — PlayerManagement.test.tsx "writes the param for a player it has just created" can fail in a full components-project run but passes in isolation — check before blaming…
- [Review batch rows the batch stages itself](reference_review_batch_rows_the_batch_stages_itself.md) — A row the review batch stages for itself (entityReviewQueue.source) is invisible to the incoming name list, so startBatch's resume reconciliation deletes it — and any…
- [Search index is or so longest token fallback is dead](reference_search_index_is_or_so_longest_token_fallback_is_dead.md) — Both Convex and convex-test full-text search are OR over query terms, so the `if (hits.length === 0) retry with longestToken(name)` fallback in teams.nearMatches and p…
- [Teardown leak attribution](reference_teardown_leak_attribution.md) — How to attribute and fix a convex-test EnvironmentTeardownError — the trace's third frame names the test file, the module name is a red herring, it reproduces ~1 full…
- [Vercel build runs convex typecheck](reference_vercel_build_runs_convex_typecheck.md) — apps/web Vercel build = convex deploy + vite build; why local vite build can pass while Vercel fails, and how to reproduce the convex typecheck locally
- [Vitest components project only tsx](reference_vitest_components_project_only_tsx.md) — apps/web's `components` vitest project globs only `*.test.tsx`, so a pure-logic `.test.ts` next to a component is never collected — name co-located pure tests `.test.t…
- [Vitest throwing console spy](reference_vitest_throwing_console_spy.md) — Two shared-fork-worker vitest flakes in apps/web from console output racing the worker lifecycle — a throwing console spy ("Worker exited unexpectedly" + lower count),…
- [Workpool oncomplete inline batched](reference_workpool_oncomplete_inline_batched.md) — @convex-dev/workpool runs onComplete callbacks INLINE and batches several into ONE transaction (Promise.all) — so a shared-doc read-modify-write across completions los…
- [Sport config is copied onto rows](sport-config-is-copied-onto-rows.md) — sportConfig defaults are COPIED onto a selectorOptions sport row at creation; editing the defaults never reaches an existing deployment's rows
- [Gates in a shared worktree](gates-in-a-shared-worktree.md) — eslint "File ignored" on lib/*.ts is baseline; wait for a sibling's npm ci; `test:unit -- filter` runs everything; link-deps.sh is broken; stale symlinked install → own npm ci in the worktree
- [Slate border contrast on slate-900](reference_slate_border_contrast_on_slate_900.md) — slate-600 still fails 3:1 on the slate-900 dialog panel (2.4:1); slate-500/gray-500 (~3.7:1) is the passing boundary tone
- [Alias index pin exempts reset drain by name and position](reference_alias_index_pin_exempts_reset_drain_by_name_and_position.md) — only ONE query("<aliasTable>") in selectorOptions.ts, after `export const reset<Table>AliasesBatch`; read aliases off the doc elsewhere
- [Client imports from convex only pure modules](reference_client_imports_from_convex_only_pure_modules.md) — a component cannot import a bound from convex/teams.ts (server graph in the bundle); hand-copy with a keep-in-step comment or let the server refuse
- [Shared-alias-only match is a question, not an adopt](reference_shared_alias_only_match_is_a_question_not_an_adopt.md) — loader adopts only when a PRIMARY name vouches; nearMatches search loop must not overwrite exact-leg candidates (drops matchedAlias)
- [Component tests hand-build the api mock](reference_component_tests_hand_build_the_api_mock.md) — `api.<newModule>` in a tested component fails every test at render; sibling-component mocks export only `default`; route reads through slot props, sentinels in pure modules
- [Maestro web text is direct text nodes only](reference_maestro_web_text_is_direct_text_nodes_only.md) — a description line in a sibling element never joins a row's `text:`; the name span stays its own match (verified from maestro-web.js 2.8.0)
- [Confirmdialog owns the word cancel](reference_confirmdialog_owns_the_word_cancel.md) — the shared ConfirmDialog always renders a button named "Cancel"; an inline picker that raises it must be a disclosure toggle, not a second Cancel
- [Inert the opener behind ConfirmDialog](reference_inert_the_opener_behind_confirmdialog.md) — not portalled and traps only Tab; use `inert` not `aria-hidden`, and restore focus in an effect because inert blurs
- [A re-parent drops a row out of its open column](reference_a_reparent_drops_a_row_out_of_its_open_column.md) — SetSelector columns are scoped to the parent id; re-point the parent column, never clear the selection
- [Wrap-boundary toolbars move under the cursor](reference_wrap_boundary_toolbar_moves_under_the_cursor.md) — fix the width, not the flow; all 4 admin screens carry it, and the width follows the longest realistic option
- [Undefined error_class is transient](reference_undefined_error_class_is_transient.md) — an untagged login failure gets the "didn't answer" copy; a test stub must set error_class to reach the refused-credentials wording
- [Convex system-op budget and bounded walks](reference_convex_system_op_budget_and_bounded_walks.md) — ~900 ops/txn comfortable, ~1800 strains, ~4000 fails; workpool component call costs ~3; cursor walk, or self-scheduled pages when no caller can loop
- [Convex typecheck excludes test files](reference_convex_typecheck_excludes_test_files.md) — convex/tsconfig excludes *.test.ts, so a changed args/returns shape is green at the gate while every test call site is stale
- [Copy that names a button goes stale](reference_copy_that_names_a_button_goes_stale.md) — a sentence quoting a control's label drifts silently; grep the JSX before trusting any label an audit or spec quotes
- [Never stash in a shared ticket worktree](reference_never_stash_in_a_shared_ticket_worktree.md) — stashing reverts siblings' edits; `git checkout --` throws away your own
- [Component test at a real cap times out](reference_component_test_at_a_real_cap_times_out.md) — a 1,000-row happy-dom fixture passes alone and times out in the suite; inject the cap as a prop
- [Refuse vs truncate a bounded read](reference_refuse_vs_truncate_a_bounded_read.md) — check the caller before writing a refusal; a window of 2 beats a budget when the consumer only counts to two; truncate a name map, never throw in a query
- [Vi mock a constant needs a getter](reference_vi_mock_a_constant_needs_a_getter.md) — lowering a MAX_* cap for a convex-test run: spread the original and expose it as a getter, or it freezes at import
- [Entity review batch is a grouping not a table](reference_entity_review_batch_is_a_grouping_not_a_table.md) — no "batches" table; the queue's reaper waits 24h but the candidates' waits 1h, and one holdings number covers both
- [Reset loop has a fifth consumer](reference_reset_loop_has_a_fifth_consumer.md) — publicFunctionAuth.test.ts pins each reset*Batch as internalMutation; the seeded toEqual block lives at two indent levels
- [OCC window moves to a query behind an action](reference_occ_window_moves_to_a_query_behind_an_action.md) — wide-range mutation losing OCC: action → internal query picks ids → internal mutation gets + re-validates; pin via ctx.db Proxy
- [dnd-kit row control is a handle sibling](reference_dndkit_row_control_is_a_handle_sibling.md) — button beside the handle, not inside; set an activator node or Enter on a child button starts a drag
- [dnd-kit pointerWithin kills keyboard drops](reference_dndkit_pointerwithin_kills_keyboard_drops.md) — use lib/dnd/keyboard-aware-collision; lib/testing/keyboard-drag runs a REAL keyboard drag in happy-dom
- [Maestro resource-id is id || aria-label](reference_maestro_resource_id_is_id_or_aria_label.md) — a DOM id on a control hides its aria-label from `id:`; find controls by data attribute
- [Subtree delete intra-table pointers](reference_subtree_delete_intra_table_pointers.md) — leaf-first paging still dangles sync-status notices (keyed on parent) and queue `source`; delete them with their target
- [Moving cards remaps the src slot key](reference_moving_cards_remaps_the_src_slot_key.md) — card src is a per-row slot key; remap via dest allocateSlots, clear an unmapped src, refuse a split pair; delete helpers exported
- [An inert trigger still names a button](reference_an_inert_trigger_still_names_a_button.md) — confirm must not reuse the trigger's text; pin unique button names with the dialog open
- [Count-gated row tears out its own dialog](reference_count_gated_row_tears_out_its_own_dialog.md) — a live-count gate unmounts the control mid-dialog; onActiveChange window; `empty:hidden` pinned by childNodes

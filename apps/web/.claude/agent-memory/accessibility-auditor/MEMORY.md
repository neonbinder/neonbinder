- [aria-disabled focus-park pattern](patterns_aria_disabled_focus_park.md) — popovers here use aria-disabled (never native disabled) on buttons; native disabled force-blurs to body and closes the popover via its own blur-close handler
- [Status-message / live-region pattern](patterns_status_message_live_regions.md) — role="alert" refusal + aria-describedby preview (no aria-live); clear-before-retry for sync guards too; wire aria-describedby to BOTH the field and the button, not just one
- [Typeahead highlight outside the listbox](patterns_typeahead_highlight_outside_listbox.md) — TeamPicker/PlayerPicker share one highlightIdx across role=option rows and a trailing action button outside role=listbox; aria-selected is invalid there, aria-current is the cheap fix
- [Measured contrast ratios for this project](reference_contrast_ratios_measured.md) — verified pairs for TeamPicker's bg-white/dark:bg-gray-800 popover AND the dark-only bg-gray-900 dialog body; gray-500 fails on both, gray-400/gray-600 is the validated substitute
- [Flex-row Reflow: fixed field + flex-1 input, no wrap](patterns_flex_row_reflow_missing_wrap.md) — a fixed-width sibling ahead of a flex-1 text input needs flex-wrap or min-w-0, or it can overflow at 320px (SC 1.4.10); check any sibling component using the same pattern first

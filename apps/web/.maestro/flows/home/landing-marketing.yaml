url: ${APP_URL || "https://localhost:3000"}
name: Landing Page Marketing Content
tags:
  - marketing
  - home
---
- launchApp
- assertVisible: "Neon Binder"

# Free Tier section
- scrollUntilVisible:
    element:
      text: ".*Free Tier.*"

# Feature detail sections (4-up grid — NEO-118 added the shipping-label card)
- scrollUntilVisible:
    element:
      text: ".*Collection Tracking.*"
- assertVisible:
    text: ".*AI-Based Card Identification.*"
- assertVisible:
    text: ".*Inventory.*"
- assertVisible:
    text: ".*Shipping Labels.*"

# How It Works section
- scrollUntilVisible:
    element:
      text: "How It Works"

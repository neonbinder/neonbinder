appId: com.neonbinder.web
url: ${APP_URL || "http://localhost:3000"}/testing/sign-in?redirect=/set-selector&worker=${WORKER_INDEX || "0"}
name: "Player picker — create a brand-new player and attach it to a CUSTOM card (the gap this closes); it persists across reopen"
tags:
  - set-selector
---
# R1 — SINGLE FEATURE: the Players section of a card's detail panel is now an
# editable typeahead chip picker with a "+ Create '<name>'" escape hatch (via
# the now-public players.findOrCreate). Typing a name with no exact match and
# choosing Create makes a brand-new player and attaches it. This is SPECIFICALLY
# what lets a manually-added CUSTOM card hold players at all — structurally
# impossible before (custom cards never went through the marketplace-sync
# review wizard that creates player rows). Players used to be read-only
# here ("None linked. Add players via the marketplace fetch flow.").
# NOTE: this flow does NOT touch the fetch review wizard — the "+ Create"
# PlayerPicker path is unrelated to and unchanged by NEO-92.
#
# R2 — HARD ASSERTIONS, no silent fall-through:
#   - Precondition: the panel opened (Card name input) and the Player picker is
#     present (aria "Player picker") — a read-only Players section would have no
#     "Add player" trigger, so tapping it would fail.
#   - The typed unique name has NO existing match, so the "+ Create '<name>'"
#     option MUST appear (id "Create player <name>"); choosing it MUST produce a
#     chip whose remove button is "Remove player <name>". A create path that
#     silently no-oped would leave no chip and fail the assert.
#   - PERSISTENCE ON A CUSTOM CARD (the actual gap): after closing and
#     reopening the drawer, the chip is STILL there — proving the custom card
#     now durably holds a player. NEO-216: the picker writes `playerIds` on the
#     TAP (there is no Save button and no draft), so the drawer's own
#     "Saved Players" confirmation is asserted as the write going out, and the
#     reopen is what proves it landed.
#
# R3 — DEDUP: team-picker.yaml covers the sibling TeamPicker; signed-by-autofills
#   reads playerIds on a REAL card but explicitly notes a custom card CANNOT
#   acquire playerIds via sync — this flow proves it now can, via manual create.
#   No other flow exercises PlayerPicker. Drill/add via util-add-custom-card.
#
# WHY A UNIQUE NAME ("NBPlayer${output.ATTEMPT_TOKEN}"): guarantees no exact match in the
#   players table (per attempt), so the create option is always offered — even
#   after prior runs seeded players under this synthetic sport. findOrCreate is
#   idempotent, so a re-run is harmless.
#
# PER-WORKER DATA ISOLATION: E2E Test Sport ${WORKER_INDEX} -> 2026 -> Topps ->
#   pp-<WORKER_INDEX> (custom set, Insert VT, "Base" insert). Fully isolated; the
#   created players carry sport "E2E Test Sport <WORKER_INDEX>", never colliding
#   with another worker's picker candidates.

# ============================================================
# LAUNCH + SIGN IN
# ============================================================

- launchApp
- extendedWaitUntil:
    visible: ".*Build set parameters using marketplace APIs.*"
    timeout: 45000

# ── The minted name's single search token (SET-REGISTRY.md, "A name typed into
# a team picker must be a SINGLE search token") ──────────────────────────────
# `teams.search` / `players.search` are Convex SEARCH indexes: they match on
# TOKENS split at non-alphanumerics, so a hyphenated `Prefix-<attempt>` name
# tokenizes to ["Prefix", "<attempt>"] and every row any flow ever left behind
# under that prefix comes back as a suggestion — the per-attempt suffix buys
# nothing. Nothing deletes a team (the app has no delete-team affordance; the
# picker's "Remove team" only unlinks it from the card), so those rows stand for
# the whole run and are visible to every concurrent worker. The names below are
# therefore ONE alphanumeric token with no separator at all.
#
# The token is ATTEMPT_ID with its hyphens stripped: "w3-a1-23945" is three
# search terms, "w3a123945" is one — and it still carries the worker index, so
# the per-worker distinction the old `${WORKER_INDEX}` prefix gave is folded in
# rather than separated out.
#
# ⚠️ READ `ATTEMPT_ID`, NOT `output.ATTEMPT_ID`. The runner injects it with
# `-e ATTEMPT_ID=…`, which Maestro exposes as the binding `${ATTEMPT_ID}`; it
# never populates `output.ATTEMPT_ID`, so the older idiom always fell through to
# its 4-digit `Date.now()` fallback and two runners collided constantly.
#
# Single-quoted: the expression contains double quotes, and quoting the scalar
# keeps YAML out of it. `Date.now()` is the standalone-run fallback.
- evalScript: '${output.ATTEMPT_TOKEN = String(ATTEMPT_ID || Date.now()).split("-").join("")}'

# ============================================================
# DRILL TO PER-WORKER CUSTOM SET + ADD A CUSTOM CARD
# ============================================================

- runFlow:
    file: util-add-custom-card.yaml
    env:
      SET_NAME: "pp-${WORKER_INDEX || 0}"
      VARIANT_TYPE: "Insert"
      INSERT_NAME: "Base"
      CARD_NUMBER: "777-${ATTEMPT_ID}"
      PLAYER_NAME: "Player Picker Card ${ATTEMPT_ID}"

# Open the drawer via the Edit button.
- assertVisible:
    id: "Edit card 777-${ATTEMPT_ID}"
- tapOn:
    id: "Edit card 777-${ATTEMPT_ID}"
    waitToSettleTimeoutMs: 2000
- extendedWaitUntil:
    visible:
      id: "Card name"
    timeout: 15000

# ============================================================
# REACH THE PLAYERS SECTION (near the bottom of the drawer body)
# Players is the last editable section, above the read-only "Inherited from set"
# and the images block. The drawer is right-anchored (~480px, x>=544), so a
# viewport-centre scroll lands on the backdrop and cannot drive the drawer's
# inner scroll — we drive it with manual swipes at x=80% (inside the drawer).
# Scrolling to the bottom parks "Add player" in the upper-middle band (the
# Inherited + Images blocks sit below it), leaving room for the popover that
# opens BELOW the trigger.
# ============================================================

- swipe:
    start: "80%, 80%"
    end: "80%, 20%"
- swipe:
    start: "80%, 80%"
    end: "80%, 20%"
- swipe:
    start: "80%, 80%"
    end: "80%, 20%"

- assertVisible:
    id: "Player picker"
- assertVisible:
    id: "Add player"

# ============================================================
# CREATE A BRAND-NEW PLAYER AND ATTACH IT
# ============================================================

- tapOn:
    id: "Add player"
- assertVisible:
    id: "Search players"
- tapOn:
    id: "Search players"
- inputText: "NBPlayer${output.ATTEMPT_TOKEN}"

# No existing player matches this unique name, so the "+ Create" row appears.
- assertVisible:
    id: "Create player NBPlayer${output.ATTEMPT_TOKEN}"
- tapOn:
    id: "Create player NBPlayer${output.ATTEMPT_TOKEN}"

# The created player is attached as a chip (its remove button proves the chip).
- assertVisible:
    id: "Remove player NBPlayer${output.ATTEMPT_TOKEN}"

# ============================================================
# THE WRITE — NEO-216: choosing "+ Create" both creates the player entity
# (findOrCreate) AND links it to this card in the same gesture. There is no
# Save button; `updateCard.playerIds` goes out on the tap, and the drawer
# confirms it in its one status region.
# ============================================================

- extendedWaitUntil:
    visible: ".*Saved Players.*"
    timeout: 7000

# Close with "Done" — it only closes. It lives in the drawer's pinned footer,
# outside the scroll body and clear of the open popover, so it needs no scroll
# (R8 exception: a footer element can never satisfy centering).
- tapOn:
    id: "Done editing card"
- assertNotVisible:
    id: "Card name"

# ============================================================
# REOPEN + VERIFY PERSISTENCE (a custom card now durably holds a player)
# ============================================================

# WHERE THE PAGE IS, WHERE THE ANCHOR IS, AND WHY *UP*. "Done editing card"
# closes the drawer and leaves the page at MAXIMUM SCROLL — the three x=80%
# swipes above drive the drawer's own scroller and chain to the window once it
# bottoms out. This Edit button is a cell in the checklist row, i.e. in the LAST
# block on the page, which by then has gone off the TOP of the viewport. A DOWN
# scroll travels away from it, and CardChecklist's react-virtuoso list unmounts
# an off-screen row, so the button is not in the hierarchy at all.
# MEASURED, both halves in the same runs. Green CI 34361627641: the same button
# at y=272 before the drawer opened and y=156 after it closed, accepted at try 0.
# Run 34429321550 runner 1 (first run with NEO-260's 208px spacer): y=64 before
# (272 - 208) and, after the close, 156 - 208 = -52 — five ElementNotFound
# retries, and the failure dump shows root bounds [0,-1135][1009,625] (document
# bottom flush with the viewport bottom = maximum scroll) with
# `virtuoso-item-list` at [72,-93][767,15] holding no rendered rows.
# UP re-anchors rather than depending on the document bottoming out (R5); one
# swipe brings the row from -52 to ~261, inside the band, and the tap that
# follows lands clear of the sticky header.
- scrollUntilVisible:
    element:
      id: "Edit card 777-${ATTEMPT_ID}"
    direction: UP
    centerElement: true
    visibilityPercentage: 50
    timeout: 7000
- tapOn:
    id: "Edit card 777-${ATTEMPT_ID}"
    waitToSettleTimeoutMs: 2000
- extendedWaitUntil:
    visible:
      id: "Card name"
    timeout: 15000

# Reach the Players section again and confirm the chip survived close + reopen.
- swipe:
    start: "80%, 80%"
    end: "80%, 20%"
- swipe:
    start: "80%, 80%"
    end: "80%, 20%"
- swipe:
    start: "80%, 80%"
    end: "80%, 20%"

- assertVisible:
    id: "Remove player NBPlayer${output.ATTEMPT_TOKEN}"

# ============================================================
# CLOSE + CLEANUP — delete this attempt's test card
# ============================================================

- tapOn:
    id: "Done editing card"
- assertNotVisible:
    id: "Card name"

- scrollUntilVisible:
    element:
      id: "Delete card 777-${ATTEMPT_ID}"
    centerElement: true
    visibilityPercentage: 50
    timeout: 7000
- tapOn:
    id: "Delete card 777-${ATTEMPT_ID}"
- assertVisible: "Confirm?"
- tapOn:
    id: "Confirm delete card 777-${ATTEMPT_ID}"
- assertNotVisible: "Player Picker Card ${ATTEMPT_ID}"

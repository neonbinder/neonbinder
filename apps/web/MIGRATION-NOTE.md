# Monorepo bring-up (NEO-18)

This repo is the consolidated NeonBinder monorepo (apps/web + services/browser),
migrated from the separate `neonbinder_web` and `neonbinder_browser` repos.
This file exists to trigger the first bring-up PR's CI (web-ci + e2e). Safe to delete.

# Afternoon autonomous session — 2026-06-02 (NEO-39/40/41/42/43)

Owner left for an appointment ~2:40pm CT. Authorized: iterate PRs to green; may merge the
form PRs (combined if needed) and any tests-only PR; may restart the dev browser service
(owner logged into gcloud; SL login works for them now); other "dev to work" PRs stay on
preview deploys. Keep this log + Linear updated in case of a crash.

## Linear map
- NEO-39 umbrella → NEO-40 (PR #48 foundation), NEO-41 (PR 2, future), NEO-42 (class-3 flow robustness), NEO-43 (prod alerting, future)
- PR #48 = jburich/neo-39-reactive-form-stability (foundation/NEO-40)
- branch jburich/neo-42-class3-flow-robustness (class-3, pushed, no PR yet)
- PR #47 = old RHF approach, to close after #48 green

## Plan
1. Restart/verify dev browser service (SL-warm was hanging earlier).
2. Verify class-3 locally (retries off) — esp. set-attributes-edit passes.
3. Merge class-3 → main (tests-only, low risk).
4. Rebase PR #48 onto main (now robust flows) → re-run E2E → green → squash-merge.
5. Close PR #47, remove neo-39-rhf-reactive-forms worktree.

## State at start
- PR #48 CI: 55 passed, 1 FAILED = set-attributes-edit (failed both attempts; class-3 toast-timing flake on SetAttributesPanel). card-features-missing/features-propagation/move-parallels "Passed on retry". → class-3 fix needed to green #48.
- Foundation correctness already proven: 135 unit tests; added card persisted to Convex; Release Date edit propagated to 122 selectorOptions rows; security-auditor PASS.
- Earlier blocker: dev browser service SL-warm hang (~10min). Service up + fast, BSC ok, only SL. Owner says SL works for them now.

## Log
- (start) Diagnosed PR #48; wrote this log; identifying dev browser service.
- Browser service: NOT actually broken. PR #48's CI (~1hr ago) PASSED test-sportlots-credentials + worker-bootstrap → SL works in CI. The earlier ~10min SL hang was a transient stuck LOCAL Cloud Run instance (since recycled). BSC + sign-in always worked. So no restart needed.
- NOTE for owner: I ran `gcloud run deploy neonbinder-browser --image <same pr-43 image>` on neonbinder-dev before realizing the above. It created revision 00101-niq at **0% traffic** (service pins default traffic by tag via the deploy pipeline), so it changed nothing serving. Harmless leftover revision; default route intact. Did NOT touch prod. No further gcloud changes made.
- Proceeding to verify class-3 locally (cascade + 5 flows, serial, retries off).

## Class-3 verification result (serial, retries off)
- 13 passed, 1 failed. SL warm worked. Class-3 fix FIXED 4/5 first-attempt: card-features-missing, edit-and-delete-card, features-propagation, set-features-panel.
- Remaining fail: set-attributes-edit Part D ("Value for League" = a FEATURE edit right after the Release Date metadata edit). League value did NOT persist (0 rows) → the feature commit raced/failed. This worktree runs origin/main's SetAttributesPanel (NOT my NEO-40 migration). Part C (Release Date metadata) committed + toast OK.
- Hypothesis: set-attributes-edit needs BOTH NEO-40 migration (fixes the metadata→feature commit race) AND class-3 flow fix (toast timing). → test PR#48 app + class-3 flows together. Owner authorized merging into one PR.

## STOP-THE-LINE FINDING — PR #48 reproduces the NEO-38 scramble (do NOT merge)
- Ran set-attributes-edit on PR#48 code (my migration) + class-3 flow, NO_DEPS, retries off → FAILED at the "Updated N cards" toast.
- Screenshot + Convex data PROVE the cross-field scramble: editing "Value for Release Date" (metadata) wrote "2024-08-01" into the `cardType` AND `signedBy` FEATURE fields across 122 descendant selectorOptions rows; RELEASE DATE left empty (—). features blob: { "cardType":"2024-08-01","league":"MLB","signedBy":"2024-08-01" }, lastUpdated = this run.
- This is the EXACT NEO-38 symptom (metadata edit fires handleSaveFeature, not handleSaveMetadata; "Updated N cards" toast can only come from the feature handler). NEO-38 was fixed in #46 on main (main passes set-attributes-edit in CI); my useReactiveField migration REINTRODUCES it.
- Root cause is NOT missing keys (features keyed by feat.key; metadata rows fixed positions) and NOT the parent onSave routing (correct in code: Release Date → onSave({releaseDate})). It is a runtime reactive re-render race that the single-field focus-guard primitive does not address — likely the uncontrolled commit-on-blur re-associating the typed value with a different row's onSave under reactive load. main's inline-uncontrolled approach does not exhibit it (passes CI); the generalization does.
- DECISION: merge NOTHING. PR #48 not ready (regresses NEO-38). Class-3 (NEO-42) flow fix is GOOD for 4/5 flows (verified first-attempt green) but set-attributes-edit can't go green until the scramble is fixed.
- OPEN QUESTION for owner: confirm main vs PR#48 under equivalent load (regression vs incomplete original fix) on a FRESH Convex preview — shared dev focused-fox-53 is contaminated with scrambled rows from today's runs.
- Branches preserved: jburich/neo-39-reactive-form-stability (PR #48, pushed), jburich/neo-42-class3-flow-robustness (pushed, no PR). Nothing merged.

## End-of-session summary (owner away)
MERGED NOTHING. Key outcome: PR #48 is NOT mergeable — it reproduces the NEO-38 scramble (the bug NEO-39 exists to fix). Converted #48 to DRAFT + commented; NEO-40 + NEO-42 Linear comments posted.

State:
- PR #48 (NEO-40, foundation): DRAFT. Unit tests + card-add path good; multi-field metadata→feature scramble NOT fixed. Branch pushed.
- NEO-42 class-3 flow fix: verified 4/5 first-attempt green; pushed (no PR, held — set-attributes-edit blocked by the scramble).
- PR #47 (old RHF): still open (not closed — #48 not merged).
- NEO-43: prod-alerting ticket filed earlier (separate).
- Browser service: SL was a transient local hang; recovered. I left a harmless 0%-traffic revision (00101-niq) on neonbinder-dev from a same-image redeploy; default route intact; prod untouched.

Recommended next steps for owner:
1. A/B set-attributes-edit on a FRESH Convex preview: main vs PR#48 under reactive load → confirm regression (PR#48) vs incomplete original NEO-38 fix.
2. Fix the scramble (the useReactiveField multi-field metadata→feature commit re-association under reactive re-render). Likely needs the primitive (or its SetAttributesPanel wiring) to bind commit→onSave so a reactive re-render can't re-route a typed value to another row's handler.
3. Re-verify all 5 flows first-attempt-green, then land class-3 (likely folded with the NEO-40 fix). Then remove the retry (NEO-42 final) + the rest of NEO-39 (NEO-41).

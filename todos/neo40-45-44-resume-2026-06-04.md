# RESUME — NEO-40/44/45 consolidation (PR #50) — 2026-06-04 evening

Safe stopping point for a laptop restart. Detailed running log: `overnight-consolidation-2026-06-03.md`.

## TL;DR
- **PR #50** (branch `jburich/neo-40-45-44-consolidate`, repo `neonbinder/neonbinder_convex`) consolidates
  NEO-40 + NEO-45 + NEO-44 + a NEO-47 drill-form fix.
- **Latest CI (p=3): 54 passed / 1 failed.** Only failure = `set-attributes-edit` ("Saved Release Date").
- **Worktree is CLEAN** at HEAD `925fed5` (debug instrumentation reverted). **Vite is STOPPED.**
- **Merge is HELD for the owner** (do not merge).
- Laptop was thermally/CPU stressed (Hearthstone ~49% CPU; RAM was fine). Closing Chrome + game helps most.

## Worktree / env
- Worktree path: `/Users/jburich/workspace/neonbinder-worktrees/neo-consolidate` (branch checked out there).
- Node 24.3.0 via nvm (`.nvmrc`). Java for Maestro = openjdk@21. Vite serves https://localhost:3000.
- gcloud token may need re-login on resume (`gcloud auth login`) — needed only for browser-service logs.
- Convex: branch has NO convex/ changes; Vite-only against shared dev `focused-fox-53` is fine.

## What's DONE + committed + pushed (origin HEAD = 925fed5)
- 801cc7b/9caef2f/22ee791/5361b9a/ea576f6: NEO-40 reactive-safe `ReactiveTextField`/`useReactiveField`
  primitive + SetAttributesPanel/CardFeaturesEditor migration + unique-field class + "Saved <field>" toast.
- fdda9e2/b3f8b97 + cf254f0: NEO-45 `useFieldTestClass` (CardDetailPanel/CardChecklist) + NEO-44 (CardForm,
  AttachSetsDialog, VariantMetadataEditor, ReconciliationModal). UnknownEntitiesDialog skipped (checkboxes only).
- b323c80/eb1da09/58d9ada: per-worker custom-set data isolation (set/card/features-propagation), folded
  set-features-panel.
- **dc32ec6 (NEO-47 fix):** 5 drill forms (Sport/Year/Manufacturer/Set/SetVariant) now go idle on an EMPTY
  marketplace sync (`optionsCount===0` / `totalSets===0`), not only on success → "+ Custom" reachable.
  This FIXED the entire custom-drill flake class (6 of the 7 prior failures). Real UX win too.
- 925fed5: 11 component tests locking in the empty-sync onDone behavior. Unit suite = 146/146 green. tsc clean
  (only 4 pre-existing errors in app/u/[username]/page.tsx + vite.config.ts).
- CI history on #50: p=3 → 56/1 (only set-attributes-edit); p=2 → 49/7; p=1 → 7 fails (ALL the drill flake,
  NOT parallelism); after the YearForm fix p=3 → **54/1** (only set-attributes-edit). CI is back at p=3.

## THE ONE REMAINING FAILURE — `set-attributes-edit` "Saved Release Date"
**Symptom:** Part C types sentinel `1900-01-01` into "Value for Release Date", presses Enter, asserts the
optimistic `.*Saved Release Date.*` toast → toast never appears (15s), both attempts. Fails in CI, PASSES
locally + at human speed. The field shows `1900-01-01` (value landed — targeting is fine), not disabled,
no toast.

**Established facts (don't relitigate):**
- The toast is OPTIMISTIC (`setToast` BEFORE the `await` in `handleSaveMetadata`, SetAttributesPanel.tsx ~L220)
  → backend slowness does NOT explain it. No toast ⇒ `handleSaveMetadata` was never called ⇒ the
  `useReactiveField` commit (`runCommit → onSave`) didn't fire.
- Attempt 1 starts from an EMPTY field, so it's NOT the no-op-on-retry. (On retry it ALSO no-ops because the
  sentinel is fixed — a secondary flow flaw, but not the root.)
- NOT parallelism (fails at p=1 too), NOT credentials/browser-service (Cloud Run logs show BSC+SL auth
  healthy on serving revision 00099-men), NOT the YearForm/drill issue (it reaches Part C fine).
- This is a long-standing CI-only flake on this flow (fought in prior sessions).

**Open question (what to nail next):** WHY doesn't `runCommit→onSave` fire in CI? Candidates:
(a) Maestro CDP `pressKey: Enter` not landing as React `onKeyDown` in CI headless; (b) a reactive
re-render race dropping the commit (NEO-36/40 class); (c) focus lost before Enter so neither onKeyDown
nor onBlur commits; (d) `busyRef` stuck. **Confidence that a blind "blur-commit" flow fix works: LOW (~30%)**
— it only addresses (a)/(c)-partial. Do NOT push a blind fix.

**Plan to finish (pick one on resume):**
1. **Local instrument repro (preferred, laptop-free of Maestro):** Re-add the temporary `[RFInstr]` logging to
   `useReactiveField.ts` (onFocus/onKeyDown/onBlur/runCommit ENTER/NO-OP/EARLY-RETURN/-> onSave) gated on a
   `data-rfinstr` marker, + a `handleSaveMetadata CALLED` log. Open Chrome at 1024×629, auth via
   `/testing/sign-in?redirect=/set-selector`, drill to a setName set, open "Edit attributes", tag the Release
   Date input `data-rfinstr=1`, pull the Convex client onto `window.__convexClient` (scan fiber for
   `.mutation`+`.watchQuery`), then: (i) edit normally → capture clean success path; (ii) edit while firing a
   `window.__convexClient.mutation(...)` to force reactive churn → see where the commit breaks. If the JS-driven
   Enter+churn reproduces the dropped commit → it's a REACTIVE RACE = app fix, verify locally. If it does NOT
   repro → it's Maestro-CDP-Enter = flow fix (blur-commit), verify in CI. (Today this was blocked when the
   Chrome extension disconnected mid-repro.)
2. **CI overlay (if local won't repro):** convert the `[RFInstr]` logs into an on-SCREEN visible overlay (Maestro
   screenshots capture it), push a debug commit that runs ONLY `set-attributes-edit` (temporarily point the
   workflow's run step at `npm run test:e2e:pick -- set-attributes-edit`), read the artifact, then revert the
   debug + apply the proven fix. Costs ~1 CI cycle.
- Keep the `.*Saved Release Date.*` assertion — it's the meaningful NEO-38 regression guard; do NOT weaken it.

## After set-attributes-edit is green
- Re-run full CI at p=3 → expect 55/55. Then: re-run lint, the security-auditor already PASSED (re-run if more
  code changed), update Linear NEO-40/44/45 (+ NEO-47 references), and tell the owner. **HOLD the merge.**
- Close #48 (DRAFT) + #49 (OPEN) when the owner merges #50 (this supersedes both).

## Tickets
- NEO-46 (High, Backlog): scale OUT e2e across free runners + isolated per-PR/per-shard Convex backends; spike
  third-party OSS-free runners (Blacksmith/Ubicloud/Depot) first. Durable fix for fast+reliable CI.
- NEO-47 (Medium, Backlog): harden web e2e reliability WITHIN Maestro (the drill-form empty-sync fix landed
  under this; set-attributes-edit commit reliability belongs here too). KEEP Maestro (PO-readable YAML +
  mobile returning; Playwright = TS/web-only = net-negative). Mobile is PAUSED not dead.

## 2026-06-04 evening — set-attributes-edit FIX pushed (blur-commit)
- RULED OUT app-logic race: useReactiveField unit tests already prove the commit fires on BOTH Enter
  (test ~L268) AND blur (~L240) — 17/17 green. So the commit logic is correct; the CI failure can only be
  Maestro's CDP `pressKey: Enter` not firing React onKeyDown in headless. Evidence-backed → flow fix.
- FIX (commit 2ebf058, pushed to #50): set-attributes-edit Part C now taps the read-only
  `Set metadata TCDB Set ID` div (MetadataReadonlyRow, no click handler, always present at setName level,
  zero side effects) after each metadata write to blur the input → onBlur → runCommit → optimistic toast.
  Kept `pressKey: Enter` (belt-and-suspenders) + both `.*Saved Release Date.*` assertions. Confidence ~70-75%.
- CI re-running at p=3 (run 26983800457). NOTE: the pr-watcher agent CANNOT poll (no Monitor permission) —
  use a background `gh run watch <id> --exit-status` for reliable re-invocation instead (set up for this run).
- If green → 55/55. Then finalize: re-run lint, security-auditor (PASSED earlier), update Linear
  NEO-40/44/45, tell owner. HOLD merge. If set-attributes-edit STILL fails → CI-overlay instrumentation
  (visible on-screen event log + run only that flow in CI) to nail it; do NOT touch the laptop.

## 2026-06-04 evening — REAL root cause found (toast off-screen) + fix pushed
- The blur fix (2ebf058) did NOT green it — CI still 54/1 on set-attributes-edit. The forensics overturned
  BOTH prior theories (parallelism AND Enter/blur-commit):
  * Attempt 1's log: "Value for Release Date" was EMPTY (text=—) when tapped, typed 1900-01-01, and the value
    PERSISTED — attempt 2 found the field pre-filled with 1900-01-01. So **the save fired correctly via Enter
    all along.** The commit was never broken.
  * The optimistic "Saved <field>" toast renders IN-FLOW above the feature/metadata grid (SetAttributesPanel
    L300). Part C scrolls the viewport DOWN to reach "Value for Release Date" → the toast renders ABOVE the
    fold, off-screen → Maestro's `visible` assertion (correctly) can't see it. Local passed because the scroll
    /viewport differed. Real UX bug too: a user saving while scrolled never sees the confirmation.
- FIX (commit cd4f3dd, pushed): toast now `position: fixed` (top-center, z-50, solid bg) so it's ALWAYS
  visible regardless of scroll — affects both metadata + feature toasts. Reverted the 2ebf058 blur-tap cruft
  (wrong theory). tsc clean (4 pre-existing); 146/146 unit. Confidence ~85%.
- CI re-running (run 26986485427) at p=3; reliable background `gh run watch` set (pr-watcher can't poll).
- LESSON: should have pulled the screenshot/forensics on the FIRST set-attributes-edit failure instead of
  theorizing (parallelism → blur). Cost several CI cycles. The value-persists-across-attempts check is what
  cracked it.

## 2026-06-04 ~7:35 PM CDT — ✅ GREEN. PR #50 = 55/55 at p=3.
- Toast-fixed fix (cd4f3dd) worked. CI run 26986485427: conclusion=success, e2e pass (43m34s),
  Maestro E2E pass, ZERO junit failures across 55 flows. The off-screen-toast diagnosis was correct.
- FINALIZE done: Linear NEO-40 + NEO-47 commented with outcome + the two real bugs. Security re-audit of
  the incremental diff launched (bg) — expected PASS (UI/CSS/test/flow only, no cred/auth/PII surface).
- HOLD: merge is the owner's. On merge: close #48 (DRAFT) + #49 (OPEN) — superseded. NEO-46 (scale-out)
  + NEO-47 (harden) remain for durable fast+reliable CI.
- New HARD-RULE memory written: feedback_never_assume_flaky_or_load (forensics-first; flaky/load/
  parallelism/driver banned as first diagnoses).

## Resume checklist
1. Restart laptop / close Chrome + Hearthstone.
2. `cd /Users/jburich/workspace/neonbinder-worktrees/neo-consolidate`; `nvm use`; `npm run dev` (Vite).
3. `gcloud auth login` only if you need browser-service logs.
4. Tackle set-attributes-edit via plan #1 or #2 above. Verify, push (reverting any instrumentation first!),
   launch pr-watcher, confirm 55/55 at p=3. HOLD merge.

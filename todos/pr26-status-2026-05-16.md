# PR #26 — what we have to fix today (snapshot 2026-05-16)

**Branch:** `feat/card-checklist-fetch`
**PR:** https://github.com/neonbinder/neonbinder_convex/pull/26
**Latest CI:** run 25881347730, commit `e2b9536`, 2026-05-14 20:01Z
**Result:** Maestro E2E — 36 tests, 34 passed, **2 failed**, 0 skipped
**Caveat:** 9 flows are gated `wip` — hard rule says PR isn't ready until they're un-`wip`'d and passing.

---

## A. Actively failing tests (latest CI run)

### A1. `checklist-fetch-unknown-entities-skip-some` — `Assertion is false: ".*USPlayerA-neontester-1778787589.*" is visible`

- File: `.maestro/flows/set-selector/checklist-fetch-unknown-entities-skip-some.yaml:94`
- Flow types `USPlayerA-${TEST_USERNAME}, USPlayerB-${TEST_USERNAME}` into the new-card-name field (line 88), then expects the **unknown-entities confirmation dialog** to show those names back. The assertion is failing → dialog either didn't open, or didn't render the typed names.
- **Linear:** NEO-15 (closed Done 2026-05-13) was about a *different* symptom on the same file (`Add` button missing). The current failure is a **new symptom**, not a NEO-15 regression.
- **In-flight fix in code?** Commits `1390451` + `e2b9536` (NEO-13) re-worked the confirm-dialog selectors and disabled Sentry/PostHog in E2E. NEO-13's own flow (`checklist-keyboard-only-dialog`) is now passing — so the dialog *does* open. The likely remaining cause here is that BSC/SL returns the typed players as *known* entities (so they never end up in the unknown-entities dialog), OR a heavy-page CDP timing issue. **No targeted fix in code yet.** Needs fresh diagnosis off the artifacts — no speculation.

### A2. `checklist-renders-rich-fields` — `Assertion is false: "BSC" is visible`

- File: `.maestro/flows/set-selector/checklist-renders-rich-fields.yaml:96`
- After tap-Refresh, the test expects at least one `BSC` platform badge on the card rows. None present → BSC adapter is returning 0 cards for Topps Chrome 2024 Base on CI.
- **Linear:** NEO-14 (Backlog, open). Diagnosis matches exactly.
- **In-flight fix in code?** Commits `3aecd4e` and `8eb4fbd` added **diagnostic logging only** (PostHog event + body preview on zero-result fetch). **No real fix yet** — we need a CI run's logs to read what BSC returned before any code change. Don't speculate on rate-limit/cookies/etc; pull the logs first. (BSC has no rate limit — confirmed by BSC.)

---

## B. Wip-tagged flows (must un-`wip` before PR ships)

Per the hard-rule on `wip` tags, these 9 must be un-tagged and made to pass. None of them ran in the latest CI count because they're filtered out.

| # | Flow | Suspected Linear |
|---|---|---|
| 1 | `set-selector/empty-state.yaml` | (none yet) |
| 2 | `set-selector/card-checklist-sync.yaml` | NEO-8 family (heavy page) |
| 3 | `set-selector/full-hierarchy-drill-down.yaml` | NEO-8 family |
| 4 | `set-selector/sync-without-credentials.yaml` | NEO-9 (creds pollution) |
| 5 | `set-selector/refresh-sportlots-creds.yaml` | NEO-9 family |
| 6 | `set-selector/add-custom-card-to-checklist.yaml` | NEO-8 family |
| 7 | `set-selector/edit-and-delete-card.yaml` | NEO-8 family |
| 8 | `set-selector/custom-entry.yaml` | NEO-10 (Maestro inputText on controlled input) |
| 9 | `set-selector/cascade/sets-inserts.yaml` | NEO-8 family |

Other flows previously listed in `todos/overnight-progress-2026-05-10-pr26-wave18.md` as `wip` (cards-base, checklist-keyboard-only-dialog, checklist-renders-rich-fields, checklist-fetch-bsc-no-seller-id, custom-entry-survives-resync, parallel-grouping-reject-parallel) have already been un-`wip`'d via commit `63e8c4d`. Of those, the keyboard-only-dialog flow now passes; the others either pass intermittently or are A1/A2 above.

---

## C. Open Linear tickets (status as of 2026-05-16)

Backlog / open:
- **NEO-8** Virtualize CardChecklist (~99 rows blow up Maestro CDP) — likely root of several wip flows in §B
- **NEO-9** `checklist-fetch-bsc-no-seller-id` pollutes worker BSC creds — flow currently passing in CI but creds pollution still real
- **NEO-10** `custom-entry-survives-resync` — Maestro `inputText` doesn't fire React onChange on controlled input
- **NEO-11** `parallel-grouping-reject-parallel` — "No changes yet" diff logic mismatch
- **NEO-13** `checklist-keyboard-only-dialog` — **fix in code, flow passing in latest run**; ticket can be closed
- **NEO-14** `checklist-renders-rich-fields` — BSC returns 0 cards. **§A2 above.** No real fix yet.

Done but worth a sanity check:
- **NEO-12** variantType auto-sync — parallelized via `c25624b`. Cascade tests pass in latest run, so this is good.
- **NEO-15** Add button missing — `Add` button fix shipped (`4f92148`, `4433bd9`, `e596c04`). Current §A1 failure is a different symptom on the same file.

Not blockers for PR #26 (lower priority / unrelated):
- NEO-7 (profile flow speed), NEO-6 (multi-version set mappings), NEO-5 (Terraform IaC for Vercel/GitHub).

---

## What this means for "today"

The **only two things that must change** for PR #26 to go green are:

1. Diagnose + fix **A1** (`checklist-fetch-unknown-entities-skip-some` — `USPlayerA` not visible). No proven cause yet. Pull the artifact log + screenshot before proposing anything.
2. Diagnose + fix **A2** (`checklist-renders-rich-fields` — `BSC` badge missing, NEO-14). Pull Cloud Run + Convex logs from the failing run; no rate-limit assumption.

After 1 + 2 are green, **then** the §B wip backlog has to be cleared (each flow un-`wip`'d and passing) before the PR can ship.

## How to resume after reboot

1. Open this file: `todos/pr26-status-2026-05-16.md` (in the `neonbinder/` root, not inside any subproject).
2. The plan file is also at `~/.claude/plans/we-ve-been-working-on-zazzy-crayon.md` (same content).
3. Latest CI run to pull artifacts from: GitHub Actions run **`25881347730`** on commit `e2b9536`.
4. Resume by tackling **A1** and **A2** — pull artifacts, then logs, then propose fixes. Do not start on §B until A1+A2 are green.

---

## D. Update 2026-05-16 — local re-run findings (diagnostic plan executed)

Executed the diagnostic plan at `~/.claude/plans/we-just-wrote-a-generic-narwhal.md`:

### D.1 — CI artifact for the failing flows is NOT available
- `gh api .../artifacts/7003587233/zip` (the only artifact on run 25881347730) contains debug folders only for the 21 *passing* flows. The two failing flows' debug folders are missing.
- The `if: failure()` extra debug upload at `.github/workflows/e2e-tests.yml:219-228` did NOT fire because the Maestro step is configured with `fail_on_failure: false`, so the job step exits 0 even when flows fail. Result: no screenshots, no per-flow commands JSON.
- **Workflow gap to fix later (not blocking PR #26):** change the upload condition so it runs when any individual flow failed, not just when the step exits non-zero. Or drop `fail_on_failure: false`.

### D.2 — PostHog & Convex historical logs are not directly queryable
- Only the client-side write key (`phc_…`) is in env; no personal API key for PostHog read API.
- `npx convex logs --history N` only streams forward; it does NOT return logs from the past.
- Both could be reached via the dashboards in Chrome (Convex dashboard / PostHog UI), but local re-run gave us what we needed faster.

### D.3 — Local re-run on commit `e2b9536` (same commit CI tested): **both flows PASS**
Local setup: Node 22.12.0, `VITE_DEV_DISABLE_HTTPS=1 npm run dev`, pointing at dev Convex (`focused-fox-53`) and dev browser service. Fresh TEST_USERNAME timestamps. Env from `.env.test`.

- **A2 (`checklist-renders-rich-fields`):** PASS. `[fetchBscChecklist] returned=335 kept=335 status=200` for filters `{"setName":["topps-chrome"],"sport":["baseball"],"year":["2024"],"variant":["base"]}`. "BSC" badge assertion at line 96 succeeded.
- **A1 (`checklist-fetch-unknown-entities-skip-some`):** PASS. Form-field assertion at line 94 (`.*USPlayerA-...*` visible) succeeded. Whole flow including the dialog interaction and "Saved N cards" assertion succeeded end-to-end.
- Local debug output saved under `/tmp/pr26-local-rerun/{a1,a2}-*`.

### D.4 — What this means

The bug is **not in PR #26's code at commit e2b9536**. The exact same code passes locally. The failures are specific to the CI environment / parallel-worker execution.

**Most likely CI-specific cause (from the failure log):** worker w1 ran `checklist-fetch-cancel-dialog` immediately before `checklist-renders-rich-fields` (back-to-back on the same worker). Per `feedback_no_flake_excuses`, this is a real bug — but it lives in **flow-to-flow state isolation**, not in the application code under review on this PR. Possibilities to investigate:
- Cancel-dialog leaves the new-card form open / focus state polluted, so when rich-fields' Refresh→dialog assertion runs, the UI is on a different page.
- BSC adapter token cached per-user in Convex is invalidated mid-suite by an unrelated worker bootstrap.
- Cross-flow shared-state pollution in dev Convex (selectorOptions / cardChecklist / players).

### D.5 — Recommended next moves (in order)

1. **Verify reproducibility on CI**: push an empty commit to the PR branch (or "Re-run failed jobs" on run 25881347730) and watch a fresh run. If both flows now pass, we have evidence the failure is non-deterministic in CI. Cost ≈ free.
2. **If they still fail on re-run**: investigate the cancel-dialog → rich-fields adjacency in `run-e2e-smoke.sh` and consider tagging `isolated:true` on the failing flows (or splitting them onto separate workers). This addresses the *real* underlying bug.
3. **Fix the artifact-upload gap**: per D.1, make `maestro-debug-output` upload reliably on per-flow failure so future investigations have screenshots without local re-run.
4. **Then move to §B `wip` backlog** (NEO-8 family).
